# Abadd0n — WSL2 / native Linux training stack
#
# Prerequisite: PyTorch with CUDA matching your driver (WSL2 usually cu128).
# Install from https://pytorch.org if needed, e.g.:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# Then (from repo root, venv activated):
#   pip install -r linux/requirements_wsl.txt
#
# Pins match the Windows venv: transformers 5.x breaks Unsloth/torchao import order.
# rich/readchar: CLI slash menu, Tab quick actions, panels.
# googlesearch-python, beautifulsoup4, requests: /search + interactive web search (no API key).
# /math uses stdlib only.

# Match Windows requirements; tested with Unsloth 2026.3.x
unsloth>=2026.3.0
unsloth-zoo>=2026.3.0
trl>=0.24.0,<0.30
transformers==4.57.6
huggingface_hub==0.36.2
datasets>=4.3.0
peft>=0.18.0
bitsandbytes>=0.49.0
spinners==0.0.24
rich>=13.0.0
readchar>=4.0.0
# Web search (Google, no API key)
googlesearch-python>=1.2.0
beautifulsoup4>=4.9
requests>=2.20
